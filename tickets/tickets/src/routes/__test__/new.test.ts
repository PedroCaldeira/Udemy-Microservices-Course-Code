import request from 'supertest';
import { app } from '../../app';

it('should exist a route handler listening to /api/tickets for post requests', async () => {});
it('should only be accessable if the user is signed in', async () => {});
it('should return an error if an invalid title is provided', async () => {});
it('should return an error if an invalid price is provided', async () => {});
it('should create tickets when given valid input', async () => {});
